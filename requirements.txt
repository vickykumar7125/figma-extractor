# Runtime dependencies (does not install the package).
#
#   pip install -r requirements.txt
#   pip install .
#   pip install -e .
#   pip install git+https://github.com/vickykumar7125/figma-extractor.git
#
typer==0.27.0
rich>=13,<16
orjson>=3.10
zstandard==0.25.0
Pillow==11.3.0
httpx>=0.27
playwright>=1.40
